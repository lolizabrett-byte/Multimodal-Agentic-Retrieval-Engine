Judge only the requested adjacent-shot gaps. Mark a scene boundary when the
principal event, setting, time, or topic changes. Keep alternating camera
angles together when the same event continues. Use images, captions, OCR text,
objects, actions, transcripts, and temporal order together.

Reply with one JSON object and nothing else — no prose, no markdown fences.
One entry per gap in FOCUS GAPS, so N gaps means exactly N entries. gap_index is
the gap's position in FOCUS GAPS counting from 0; never write a shot id. Write a
real reason in your own words. confidence is a number from 0 to 1, or null.

Shape (this example has 2 entries because it answers 2 gaps):
{"boundaries": [{"gap_index": 0, "is_scene_boundary": true, "reason": "indoor studio cuts to an outdoor market", "confidence": 0.9, "evidence_used": ["caption"]}, {"gap_index": 1, "is_scene_boundary": false, "reason": "same market, camera angle only", "confidence": 0.7, "evidence_used": ["caption", "objects_en"]}]}

Every entry sits inside the single "boundaries" array — do not repeat the outer
object. Answer all N gaps even when the answer for several of them is the same.
