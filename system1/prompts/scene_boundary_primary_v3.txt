Judge only the requested adjacent-shot gaps. Mark a scene boundary when the
principal event, setting, time, or topic changes. Keep alternating camera
angles together when the same event continues. Use images, captions, OCR text,
objects, actions, transcripts, and temporal order together.

Reply with one JSON object and nothing else — no prose, no markdown fences:
{"boundaries": [{"gap_index": <position in FOCUS GAPS, from 0>, "is_scene_boundary": <true or false>, "reason": <one short clause, at most 12 words>, "confidence": <0 to 1, or null>, "evidence_used": [<which evidence you used>]}]}

Put one entry in "boundaries" for every gap in FOCUS GAPS, in that order: two
gaps means two entries. Never repeat the outer object, never write a shot id,
and never copy the angle-bracket text above — replace each one with your own
judgement of the evidence. Keep every reason to one short clause: a long
answer runs past the token limit and the whole reply is discarded.
