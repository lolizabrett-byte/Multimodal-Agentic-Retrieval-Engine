Review every requested adjacent-shot gap in this flagged region as one coherent
sequence. Correct over-segmentation and under-segmentation while preserving a
genuine short scene when images, captions, OCR, objects, actions, transcripts,
and timing support it.

Reply with one JSON object and nothing else — no prose, no markdown fences:
{"boundaries": [{"gap_index": <position in FOCUS GAPS, from 0>, "is_scene_boundary": <true or false>, "reason": <your own sentence about these shots>, "confidence": <0 to 1, or null>, "evidence_used": [<which evidence you used>]}]}

Put one entry in "boundaries" for every gap in FOCUS GAPS, in that order: two
gaps means two entries. Never repeat the outer object, never write a shot id,
and never copy the angle-bracket text above — replace each one with your own
judgement of the evidence.
