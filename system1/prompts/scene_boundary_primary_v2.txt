Judge only the requested adjacent-shot gaps. Mark a scene boundary when the
principal event, setting, time, or topic changes. Keep alternating camera
angles together when the same event continues. Use images, captions, OCR text,
objects, actions, transcripts, and temporal order together.

Reply with one JSON object and nothing else — no prose, no markdown fences:
{"boundaries": [{"after_shot_id": "<shot id as given>", "is_scene_boundary": true, "reason": "<short reason>", "confidence": 0.8, "evidence_used": ["caption"]}]}

Return exactly one entry per requested gap, in the order given, reusing the
shot ids verbatim. confidence is a number from 0 to 1, or null when unsure.
